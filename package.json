{
  "name": "mcss-api",
  "version": "1.0.0",
  "description": "Simple package to interact with MCSS' API",
  "main": "index.js",
  "scripts": {
    "test": "node ./test/blob.js",
    "build": "npx tsc ./src/index.ts",
    "prepare": "npm run build",
    "dev": "pnpm exec swc src --config-file .swcrc -d dist/  && node dist/index.js",
    "start": "node dist/index.js"
  },
  "author": "PouletteMC",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "axois": "^0.0.1-security",
    "jest": "^28.1.3",
    "pnpm": "^7.6.0"
  }
}
