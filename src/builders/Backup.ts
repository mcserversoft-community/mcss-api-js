
export enum Compression { // THIS IS FALSE
    HIGH = 'high',
    LOW = 'low',
    NONE = 'none'
}

export default class Backup {
    constructor(name: string, destination: string, ignore: string[]) {
        // TODO: Implement
    }
}
